# ECPay 物流選店電子地圖 — 用於購皂頁的「選擇門市」popup。
# 流程：popup → POST 到 ECPay → 使用者選店 → ECPay POST 回 /api/store-callback
# → callback HTML 透過 postMessage 把店號帶回主頁，自動填入表單。

# 部署正式環境的 MerchantID 走 Cloudflare Workers dashboard 的
# Variables and Secrets（runtime env）：設 ECPAY_MERCHANT_ID=<你的商編>。
# 前端會在表單載入時 fetch /api/config 拿這個值，dashboard 改值即時生效。

# 本檔下方變數只在「本地 vite dev server」會用到 — vite dev 不走 worker，
# 所以 /api/config 抓不到，退回讀這份 .env.local 當 fallback。
VITE_ECPAY_MERCHANT_ID=2000132
VITE_ECPAY_EMAP_URL=https://logistics-stage.ecpay.com.tw/Express/map
